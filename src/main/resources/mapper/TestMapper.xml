<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.park.firstbatch.jobs.TestMapper">
    <insert id="insertData1" parameterType="String">
        insert into test_a (title)
        values (#{data})
    </insert>

    <insert id="insertData2">
        insert into test_a (title)
        values (#{data})
    </insert>
</mapper>